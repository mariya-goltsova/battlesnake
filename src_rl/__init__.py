"""Self-contained Battlesnake RL package: simulator, safety, features, policy, training.

Root project files (backend.py, logic.py) are intentionally untouched; this package
has its own serving entry point (backend_rl.py) and training pipeline (train/).
"""
